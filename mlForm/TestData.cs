namespace mlForm
{
    class TestData
    {
        internal static readonly IrisData prediction = new IrisData  //Tahmin yapması için test değerleri verdik.
        {
            SepalLength = 2.5f,
            SepalWidth = 5.0f,
            PetalLength = 1.0f,
            PetalWidth = 3.0f,
        };
    }
}
